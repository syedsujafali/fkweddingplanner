export interface Service {
  id: string;
  title: string;
  description: string;
  image?: string;
}

export interface WeddingSpecialty {
  id: string;
  title: string;
  description: string;
  image?: string;
}

export interface OtherEvent {
  id: string;
  title: string;
  description: string;
  image?: string;
}

export interface WhyChooseItem {
  id: string;
  title: string;
  description: string;
  image?: string;
}

export interface ProcessStep {
  id: string;
  number: string;
  title: string;
  description: string;
  image?: string;
}

export interface DecorItem {
  id: string;
  title: string;
  description: string;
  image?: string;
}

export interface CateringItem {
  id: string;
  title: string;
  description: string;
  image?: string;
}

